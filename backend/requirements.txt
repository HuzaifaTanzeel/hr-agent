# FastAPI and Web Framework
fastapi
uvicorn[standard]
pydantic
pydantic-settings
python-multipart

# Database
sqlalchemy
asyncpg

# Security
passlib[bcrypt]
python-jose[cryptography]
python-dotenv

# Utilities
email-validator
markdown

# RAG and Embeddings
# Note: torch is installed separately in Dockerfile for CPU-only version
sentence-transformers
chromadb

openai-agents